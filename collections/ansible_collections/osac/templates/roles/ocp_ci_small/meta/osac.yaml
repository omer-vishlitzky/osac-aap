title: CI Hosted Cluster
description: >
  Provisions a hosted cluster for CI testing. Skips ESI networking — agents
  must already be on the same network as the hub.

default_node_request:
- resourceClass: ci-worker
  numberOfNodes: 1

allowed_resource_classes: []
